---
import Svg from "../assets/svg/Data_storage_Monochromatic_2.svg";
import Section from "../components/Section/Section.astro";

const sectionTitle = "backend";

const services = [
  {
    name: "API Development",
    description:
      "RESTful and GraphQL APIs with proper authentication and validation",
    icon: "🔌",
    color: "var(--pico-primary)",
  },
  {
    name: "Database Design",
    description:
      "Optimized schemas and queries for performance and scalability",
    icon: "🗄️",
    color: "var(--pico-secondary)",
  },
  {
    name: "Cloud Architecture",
    description:
      "Scalable and cost-effective solutions on AWS and other platforms",
    icon: "🏗️",
    color: "var(--pico-tertiary)",
  },
  {
    name: "DevOps & CI/CD",
    description: "Automated deployment pipelines and infrastructure as code",
    icon: "🚀",
    color: "var(--pico-primary)",
  },
];
---

<Section
  id={sectionTitle}
  svg={Svg}
  svgAlt="Server development illustration"
  title={sectionTitle}
  inverse
>
  <div class="backend-content">
    <p>
      I use Node.js to create powerful and scalable server-side applications.
      Though my Java skills are still in development, I'm always expanding my
      expertise. I also have experience with AWS, enabling me to design and
      deploy cloud-based solutions that are both reliable and cost-effective.
    </p>

    <div class="services-section">
      <h3>Backend Services</h3>
      <div class="services-grid">
        {
          services.map((service) => (
            <div
              class="service-card"
              style={`--accent-color: ${service.color}`}
            >
              <div class="service-icon">{service.icon}</div>
              <h4 class="service-title">{service.name}</h4>
              <p class="service-description">{service.description}</p>
            </div>
          ))
        }
      </div>
    </div>
  </div>

  <style>
    .backend-content {
      display: flex;
      flex-direction: column;
      gap: 2rem;
    }

    .skills-section,
    .technologies-section,
    .services-section {
      margin-top: 1.5rem;
    }

    .skills-section h3,
    .technologies-section h3,
    .services-section h3 {
      color: var(--pico-primary);
      margin-bottom: 1rem;
      font-size: 1.2rem;
    }

    .skills-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 1rem;
    }

    .skill-card {
      background: var(--pico-card-background-color);
      border: 1px solid var(--pico-muted-border-color);
      border-radius: var(--pico-border-radius);
      padding: 1rem;
      transition:
        transform 0.2s ease,
        box-shadow 0.2s ease;
    }

    .skill-card:hover {
      transform: translateY(-2px);
      box-shadow: var(--pico-card-box-shadow);
    }

    .skill-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 0.5rem;
    }

    .skill-name {
      font-weight: 600;
      color: var(--pico-color);
    }

    .skill-percentage {
      color: var(--pico-muted-color);
      font-size: 0.9rem;
    }

    .skill-bar {
      height: 8px;
      background: var(--pico-muted-border-color);
      border-radius: 4px;
      overflow: hidden;
    }

    .skill-progress {
      height: 100%;
      border-radius: 4px;
      transition: width 1s ease-in-out;
    }

    .tech-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
      gap: 0.75rem;
    }

    .tech-item {
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 0.75rem;
      background: var(--pico-card-background-color);
      border: 1px solid var(--pico-muted-border-color);
      border-radius: var(--pico-border-radius);
      transition:
        transform 0.2s ease,
        background-color 0.2s ease;
      text-align: center;
    }

    .tech-item:hover {
      transform: translateY(-2px);
      background: var(--pico-primary-background);
    }

    .tech-icon {
      font-size: 1.5rem;
      margin-bottom: 0.25rem;
    }

    .tech-name {
      font-size: 0.8rem;
      color: var(--pico-color);
      font-weight: 500;
      margin-bottom: 0.25rem;
    }

    .tech-category {
      font-size: 0.7rem;
      color: var(--pico-muted-color);
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .services-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 1rem;
    }

    .service-card {
      background: var(--pico-card-background-color);
      border: 2px solid var(--accent-color);
      border-radius: var(--pico-border-radius);
      padding: 1.5rem;
      text-align: center;
      transition:
        transform 0.2s ease,
        box-shadow 0.2s ease;
      position: relative;
      overflow: hidden;
    }

    .service-card::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 4px;
      background: var(--accent-color);
    }

    .service-card:hover {
      transform: translateY(-4px);
      box-shadow: var(--pico-card-box-shadow);
    }

    .service-icon {
      font-size: 2.5rem;
      margin-bottom: 1rem;
    }

    .service-title {
      margin: 0 0 0.75rem 0;
      color: var(--pico-color);
      font-size: 1.1rem;
      font-weight: 600;
    }

    .service-description {
      margin: 0;
      color: var(--pico-muted-color);
      font-size: 0.9rem;
      line-height: 1.4;
    }

    @media (max-width: 768px) {
      .skills-grid {
        grid-template-columns: 1fr;
      }

      .tech-grid {
        grid-template-columns: repeat(auto-fit, minmax(100px, 1fr));
      }

      .services-grid {
        grid-template-columns: 1fr;
      }
    }
  </style>
</Section>
