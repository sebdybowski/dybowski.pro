---
export interface Props {
  icon: string;
  size: string;
}

const { icon, size } = Astro.props as Props;
const { default: innerHTML } = await import(`/src/assets/icons/${icon}.svg?raw`);
---

<Fragment width={size} height={size} set:html={innerHTML} />